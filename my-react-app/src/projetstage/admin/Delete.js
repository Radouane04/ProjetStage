import React from 'react';
import { useParams } from 'react-router-dom';
const Delete = ({Articles , setListe}) => {
    const {id} = useParams()
    return (
        <div>
            
        </div>
    );
}

export default Delete;
